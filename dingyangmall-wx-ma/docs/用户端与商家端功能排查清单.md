# 用户端与商家端功能排查清单（移动端已有功能）

本文档按「用户端（通用）」与「商家端」逐项对照现有后端接口与前端页面，标注 **✅ 已有** / **⚠ 部分有** / **❌ 待补充**，便于查漏补缺。

---

## 一、用户端（所有用户通用：经销商 + 终端客户）

### 1. 账户与安全

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 用户注册 | POST `/app/member/register`（手机号+验证码+密码） | 小程序以微信登录为主，C 端 H5 可走此接口 | ✅ 已有 |
| 登录 | 小程序：POST `/api/ma/wxuser/login`（code）；C 端：POST `/app/member/login`（手机+密码） | 小程序登录页、静默登录 | ✅ 已有 |
| 找回密码 | 无 | 需：短信验证码 + 新密码 重置 | ❌ 待补充 |
| 修改密码 | 后台：PUT `/system/user/profile/updatePwd`（sys_user）；C 端会员（ums_member）无 | 用户端修改密码需单独接口 | ❌ 待补充 |

**建议补充**：  
- C 端「找回密码」：如 POST `/app/member/reset-password`（phone、code、newPassword）。  
- C 端「修改密码」：如 PUT `/app/member/update-pwd`（oldPassword、newPassword），或 PUT `/api/ma/wxuser/password`（需先绑定手机且鉴权为当前用户）。

---

### 2. 个人中心

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 查看积分 | GET `/api/ma/wxuser/info`、GET `/api/ma/wxuser/member-code` 返回 points | 个人中心展示 | ✅ 已有 |
| 会员码（二维码） | GET `/api/ma/wxuser/member-code`，前端用会员码生成二维码 | 个人中心「会员码」弹窗、复制、二维码 | ✅ 已有 |

---

### 3. 收货地址管理

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 新增地址 | POST `/api/ma/useraddress` | 编辑地址页 | ✅ 已有 |
| 编辑地址 | POST `/api/ma/useraddress`（同新增） | 编辑地址页 | ✅ 已有 |
| 删除地址 | DELETE `/api/ma/useraddress/{id}` | 地址列表 | ✅ 已有 |
| 设默认 | 保存时传 `isDefault=1`，后端会将其余设为非默认 | 编辑页勾选默认 | ✅ 已有 |
| 最多 10 个 | 新增时校验当前用户地址数 < 10，超过则提示先删除再添加 | 后端已限制 | ✅ 已补充 |

---

### 4. 积分与活动

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 每日签到领积分 | POST `/api/ma/wxuser/sign-in`（小程序已改走此路径，与 X-Wx-Token 同源认证）；备选 POST `/app/member/sign-in` | 我的-签到、首页签到；后端需在 /api/ma 下提供签到或做 openid→memberId 转发 | ⚠ 需后端实现 /api/ma 签到 |
| 推荐注册领奖励 | POST `/app/member/register` 传 `inviteCode`（推荐人会员码） | 注册页填推荐人；规则在 `tb_integral_rule.recommendIntegral` | ✅ 已有 |
| 积分红包 | POST `/app/member/send-packet`，规则 `redPacketSwitch` 控制 | 积分红包页；同上需 memberId 与手机验证 | ⚠ 部分有 |

**说明**：小程序当前通过 X-Wx-Token 解析为 openid，部分接口（如 `/app/member/*`）期望 `MemberUtils.getMemberId()` 为 ums_member 的 Long id，若未做 openid→memberId 映射会报错。建议签到、积分红包等增加 `/api/ma/*` 下接口，由 token 解析 openid→phone→ums_member 再调业务。

---

### 5. 商品浏览与兑换

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 查看全部商品 | GET `/api/ma/goodsspu/page`、GET `/api/ma/goodsspu/list`、GET `/api/ma/home` | 首页、商品列表、分类 | ✅ 已有 |
| 分类 | GET `/api/ma/goodscategory/tree`、GET `/api/public/ma/goodscategory/tree` | 首页宫格、分类页 | ✅ 已有 |
| 商品详情 | GET `/api/ma/goodsspu/{id}` | 商品详情页 | ✅ 已有 |
| 实体商品兑换（选地址、生成订单） | POST `/api/ma/orderinfo` 下单，选地址 | 确认订单页、地址选择 | ✅ 已有 |
| 虚拟商品兑换（自动到账） | 下单后业务根据商品类型自动处理 | 订单/商品逻辑支持 | ✅ 已有 |
| 商品券兑换（券码、有效期） | 下单后生成 tb_coupon_info，返回券码与有效期 | 订单详情/我的商品券 | ✅ 已有 |

---

### 6. 我的订单

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 查看全部状态 | GET `/api/ma/orderinfo/page`（status 筛选） | 订单列表 | ✅ 已有 |
| 物流信息 | GET `/api/ma/orderinfo/logistics/{id}` 或订单详情内物流 | 订单详情、物流页 | ✅ 已有 |
| 确认收货 | PUT `/api/ma/orderinfo/receive/{id}` | 订单详情 | ✅ 已有 |
| **未付款订单超时自动取消** | 无接口，需后端定时任务 | 业务需求：超 30 分钟未付款订单自动取消 | ❌ 待后端实现 |

**未付款订单超时取消说明**：需后端增加定时任务（如每 5～10 分钟执行），查询 `status=待付款(0)` 且 `createTime < 当前时间 - 30 分钟` 的订单，调用现有 `PUT /orderinfo/cancel/{id}` 逻辑（更新状态、释放库存等）。前端无轮询，完全由后端处理。

---

### 7. 我的商品券

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 查看券码、状态、有效期 | GET `/api/ma/wxuser/coupons`（status：1 未使用/2 已使用/3 已过期）；备选 GET `/app/coupon/my` | 我的优惠券页优先调用 `api.couponMyMa(status)` | ✅ 已有 |
| 过期提醒 | 前端根据 validityEnd 判断 | 列表/详情展示 | ⚠ 前端可做 |

---

### 8. 抽奖

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 抽奖配置（消耗积分、次数上限） | GET `/api/ma/lottery/config`（小程序已切到此路径）；备选 `/app/lottery/config` | 抽奖页 | ⚠ 需后端实现 /api/ma 抽奖 |
| 消耗积分抽奖 | POST `/api/ma/lottery/draw`（小程序已切）；备选 `/app/lottery/draw` | 抽奖页 | ⚠ 同上 |
| 中奖记录 | GET `/api/ma/lottery/record`（小程序已切）；备选 `/app/lottery/record` | 抽奖页 | ⚠ 同上 |

**说明**：小程序抽奖接口已改为 `/api/ma/lottery/*`，与 X-Wx-Token 同源认证，避免 `/app/lottery/*` 的 401。后端需在该路径下实现或内部转发到 lottery 业务。

---

### 9. 消息通知

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 系统公告 | GET `/api/public/ma/notice/list` | 首页公告轮播 | ✅ 已有 |
| 积分变动 / 订单发货 / 券即将过期 | 暂无站内消息表或推送接口 | 可做：站内消息列表、或仅前端根据订单/券/流水轮询 | ❌ 待补充 |

**说明**：若需「消息中心」列表（积分变动、发货、券过期提醒），需后端设计消息表及 GET 消息列表、已读等接口。

---

### 10. 客服联系

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 查看客服电话/微信 | 可读 sys_config（如客服电话、客服微信） | 前端需接口获取配置 | ⚠ 前端已接 |

**说明**：移动端「我的」页已增加「联系客服」入口，调用 GET `/api/public/ma/config/service` 展示客服电话（点击拨打）、客服微信（点击复制）。后端需提供该接口并返回 `{ phone, wechat }`（或 `servicePhone`/`serviceWechat` 等字段）。

---

## 二、商家端（一级经销商 + 二级经销商）

### 1. 账户与权限

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 创建二级经销商账户（短信验证、发初始密码） | POST `/registerDistributor`（RegisterBody：手机、验证码、用户名、密码等） | 管理端或商家端创建子账号 | ✅ 已有 |
| 创建终端客户账户（短信验证、发初始密码） | 无单独「创建终端客户」接口；终端客户多为小程序注册或 ums_member 注册 | 若需商家代创建，需新增接口 | ❌ 待补充 |
| 商家登录 | 后台登录（账号密码 + 验证码），Token 给管理端/商家端 | 商家登录页 | ✅ 已有 |
| 修改密码 | PUT `/system/user/profile/updatePwd`（当前登录 sys_user） | 商家端个人设置 | ✅ 已有 |

---

### 2. 扫码与积分

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 扫码识别用户会员码 | GET `/api/mall/merchant/scan/user/{memberCode}`（会员码或手机号） | 商家端扫会员码 / 输入手机号 | ✅ 已有 |
| 给下游用户赠送积分（扫码/输手机号） | POST `/api/mall/merchant/scan/points`（memberCode、points） | 商家端赠送积分 | ✅ 已有 |
| 赠送积分需短信验证 | POST `/api/mall/merchant/scan/points` 支持 body.smsCode；配置 `mall.merchant.give-points-require-sms=true` 时必填并校验商家手机验证码 | 商家端传 smsCode 即可 | ✅ 已补充 |

---

### 3. 商品券核销

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 扫码核销、校验券状态/有效期 | POST `/api/mall/merchant/scan/coupon/verify`（couponCode） | 商家端扫券码核销 | ✅ 已有 |

---

### 4. 积分与流水

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 查看自己的积分余额 | 当前为 sys_user，若商家积分在 ums_member 需按业务查 | 若商家与会员体系打通需明确数据来源 | ⚠ 按业务 |
| 查看自己的积分流水 | GET `/integralflow/page`（管理端）；商家端按自己 userId 筛需单独接口或权限过滤 | 管理端已有；商家端若需仅看本人流水需接口 | ⚠ 部分有 |

---

### 5. 消息通知

| 功能 | 后端接口 | 前端/说明 | 状态 |
|------|----------|-----------|------|
| 消息通知、系统公告 | 同用户端，或管理端通知列表 | 商家端可复用公告或站内消息 | ⚠ 部分有 |

---

## 三、接口路径速查（用户端 / 商家端）

**用户端（小程序 / C 端）**  
- 登录/会员：`/api/ma/wxuser/login`、`/api/ma/wxuser/info`、`/api/ma/wxuser/member-code`  
- 地址：`/api/ma/useraddress/page`、POST、DELETE  
- 商品：`/api/ma/goodsspu/*`、`/api/ma/goodscategory/tree`、`/api/ma/home`  
- 订单：`/api/ma/orderinfo/*`  
- 签到：**POST `/api/ma/wxuser/sign-in`**（小程序已切到此路径，避免 401）；红包/注册：`/app/member/send-packet`、`/app/member/register`（注意 memberId 来源）  
- 优惠券/商品券：`/api/ma/wxuser/coupons`（小程序推荐）、`/app/coupon/my`  
- 抽奖：**GET `/api/ma/lottery/config`**、**POST `/api/ma/lottery/draw`**、**GET `/api/ma/lottery/record`**（小程序已切到此路径，避免 401）；备选 `/app/lottery/*`  
- 公告：`/api/public/ma/notice/list`  
- **客服配置**：`GET /api/public/ma/config/service`（移动端已接，后端待实现）  
- **找回/修改密码**：`POST /app/member/reset-password`、`PUT /app/member/update-pwd`（api.js 已预留，后端待实现）

**商家端**  
- 扫码识别：GET `/api/mall/merchant/scan/user/{memberCode}`  
- 赠送积分：POST `/api/mall/merchant/scan/points`  
- 券核销：POST `/api/mall/merchant/scan/coupon/verify`  
- 注册二级经销商：POST `/registerDistributor`  

---

## 四、建议优先补充项（汇总）

1. **用户端**  
   - ~~收货地址：保存时限制最多 10 条~~（已实现）。  
   - ~~我的商品券：GET `/api/ma/wxuser/coupons`~~（已实现，小程序「我的优惠券」页已优先使用）。  
   - 找回密码、修改密码（C 端 ums_member）：**前端 api.js 已预留** `resetPassword`、`updatePwd`，待后端提供接口。  
   - 客服联系方式：**前端已接**「联系客服」入口与 GET `/api/public/ma/config/service`，待后端实现该接口。  
   - 签到/积分红包/抽奖：保证小程序能用的 /api/ma 或统一 memberId 解析。  

2. **商家端**  
   - ~~赠送积分：增加短信验证码校验（可选/必填由配置控制）~~（已实现）。  
   - 创建终端客户：商家代创建 ums_member 并发初始密码（短信）。  
   - 商家「我的积分/流水」：按当前登录身份查询的接口（若业务需要）。  

---

## 五、移动端本次已补充项

| 项 | 说明 |
|----|------|
| **api.js** | 新增 `resetPassword`（POST `/app/member/reset-password`）、`updatePwd`（PUT `/app/member/update-pwd`）、`getServiceConfig`（GET `/api/public/ma/config/service`）。 |
| **我的页** | 新增「联系客服」菜单，点击请求客服配置并弹窗展示客服电话（点击拨打）、客服微信（点击复制）；无配置时提示「暂无客服配置」。 |
| **我的优惠券页** | 优先使用 `api.couponMyMa(status)`，列表解析兼容 `data` / `data.records` / `data.list` / `data.rows` / `data.content`，并统一字段：`couponStatus`/`status`、`couponCode`/`code`、`validityEnd`/`endTime`、`goodsName`/`name`。 |
| **每日签到** | 前端已改为请求 **POST `/api/ma/wxuser/sign-in`**（与购物车、订单等接口同源认证，避免 `/app/member/sign-in` 的 401）。后端需在该路径下实现签到逻辑（或内部转发到 member 签到并做 openid→memberId 映射）。 |
| **抽奖接口** | 前端已改为请求 **GET `/api/ma/lottery/config`**、**POST `/api/ma/lottery/draw`**、**GET `/api/ma/lottery/record`**（与 X-Wx-Token 同源认证，避免 `/app/lottery/*` 的 401）。后端需在 /api/ma/lottery 下实现或转发。 |

按上述清单逐项对接前端与后端即可查漏补缺；实现优先级可按产品排期决定。
