<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dingyangmall.web.mapper.WxMaUserMapper">

    <!-- 使用旧表 wx_user：open_id/nick_name/headimg_url/union_id，主键 id -->
    <resultMap id="BaseResultMap" type="com.dingyangmall.web.entity.WxMaUser">
        <id column="id" property="id"/>
        <result column="open_id" property="openid"/>
        <result column="union_id" property="unionid"/>
        <result column="nick_name" property="nickname"/>
        <result column="headimg_url" property="avatarUrl"/>
        <result column="phone" property="phone"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByOpenid" resultMap="BaseResultMap">
        SELECT id, open_id, union_id, nick_name, headimg_url, phone, create_time, update_time
        FROM wx_user
        WHERE open_id = #{openid}
        LIMIT 1
    </select>

    <insert id="upsert">
        INSERT INTO wx_user (id, open_id, union_id, nick_name, headimg_url, app_type, del_flag)
        VALUES (#{u.id}, #{u.openid}, #{u.unionid}, #{u.nickname}, #{u.avatarUrl}, '1', '0')
        ON DUPLICATE KEY UPDATE
            union_id = COALESCE(VALUES(union_id), union_id),
            nick_name = COALESCE(VALUES(nick_name), nick_name),
            headimg_url = COALESCE(VALUES(headimg_url), headimg_url),
            update_time = CURRENT_TIMESTAMP
    </insert>

    <update id="updatePhoneByOpenid">
        UPDATE wx_user SET phone = #{phone}, update_time = CURRENT_TIMESTAMP WHERE open_id = #{openid}
    </update>

</mapper>

